- 所有外部可配置项统一通过 `.env` 注入，compose 中使用 `${VAR:-默认值}` 语法提供本地开发默认值，避免硬编码。
- Dockerfile 采用三阶段分离：构建期装编译工具链、运行时只保留生产依赖并通过 `npm prune --omit=dev` 裁剪体积。
- 每个服务镜像都内置 `HEALTHCHECK`，compose 中通过 `condition: service_healthy` 建立显式启动顺序而非简单 `depends_on`。